You are an expert technical copywriter and web designer. Your task is to generate a complete, single-file HTML microsite about context in AI coding agents and how Sourcegraph MCP solves the context problem.

The microsite must be production-quality HTML/CSS/JS. Follow these design principles:
- Dark theme: background #0D1117, card backgrounds #161B22, accent color #FF5543 (Sourcegraph brand red), secondary accent #A78BFA (purple)
- Clean sans-serif typography (Inter or system-ui), generous whitespace, subtle border treatments (#30363D)
- Smooth scroll animations using IntersectionObserver
- Responsive layout (mobile-friendly)
- No external dependencies except Google Fonts

The microsite must contain these sections in order below is landing page

---

## 1. HERO
Bold headline: "Your AI is only as smart as its context."
Subheadline explaining that most AI coding assistants are flying blind — working with only the files currently open, missing the full picture of your codebase.
CTA button: "See How It Works" (smooth scrolls to next section).

---

## 2. THE CONTEXT PROBLEM
Visual explanation (use SVG or CSS diagrams, no images) showing the difference between:
- "Narrow context" — AI only sees open files, gets confused, hallucinates APIs, misses patterns
- "Rich context" — AI understands the whole codebase, dependencies, history, conventions
Use a two-column comparison card layout.
Break this out into a separate problem create a page on its own with details: talk about context  -  why its the differentiator use this medium blog as a reference
https://medium.com/@ajaynz/the-software-factory-is-here-context-is-still-missing-90a527b0e0fe?postPublishedType=repub
---

## 3. HOW MCP WORKS
A step-by-step explainer of the Model Context Protocol. Cover:
- What MCP is (an open protocol from Anthropic that standardises how AI models connect to data sources and tools)
- The client–server architecture (Host / MCP Client / MCP Server / Data Source)
- The three primitives: Resources (read-only data), Tools (executable actions), Prompts (reusable templates)
- How an agent "pulls" context on demand rather than having everything stuffed in a prompt upfront

Include a horizontal flow diagram built in SVG showing:
Agent → MCP Client → MCP Server → Sourcegraph → Codebase

Develop a dedicated MCP guide on how to use MCP with Sourcegraph tool calling use the a.pdf file in this directory as a reference



---

## 4. HOW AGENTS GATHER CONTEXT
Explain the context-gathering loop:
1. Planning — agent identifies what it needs to know
2. Discovery — MCP server exposes available resources and tools
3. Retrieval — agent calls tools (e.g. search_code, get_file, find_references)
4. Synthesis — agent assembles retrieved context into its reasoning window
5. Action — agent produces grounded, accurate output

Show this as an animated circular loop diagram (CSS animation).
I want you to take design inspiration from https://background-agents.com/ but stick to Sourcegraph theme.

---

## 5. SOURCEGRAPH MCP IN PRACTICE
Concrete walkthrough of Sourcegraph as an MCP server. Include:
- Key capabilities: universal code search across every repo, precise code navigation (go-to-definition, find-references), code intelligence, deep search
- Why this matters: agents using Sourcegraph MCP can answer "where is this API used?", "what's the correct pattern for auth in this codebase?", "which teams own this service?"
- Configuration snippet (show a JSON code block):
Use https://sourcegraph.com/integrations as reference
- Supported hosts: Claude Desktop, Cursor, Windsurf, VS Code (Copilot), any MCP-compatible agent
- Explain that you can use sourcegraph MCP against https://sourcegraph.com/search and use for opensource repos
---

## 6. USE CASES
Three cards with icons (use emoji or inline SVG icons):
- Understanding Existing Code 
- Consistency & Quality 
- Codebase-wide Refactoring 
- fixing bug 
- Implementing new feature
- Code review	
- debugging issues/troubleshooting 







Ralph loop round: 2 of 5

Instructions:
- Inspect the current workspace state before making changes.
- Continue from the current repository state, not from assumptions.
- Prefer concrete edits over long explanations.
- End with a short status summary of what changed and what remains.

Previous round final message:

Built a production-style single-file microsite at [index.html](/Users/asridhar/work/Sourcegraph/projects/microsite/index.html). It includes the required dark Sourcegraph-themed design, six sections in order, inline SVG diagrams, a page-like context problem chapter, an MCP explainer, an animated context-gathering loop, Sourcegraph MCP configuration and capabilities, and responsive scroll/reveal behavior with `IntersectionObserver`.

I also validated the file parses as HTML and confirmed the required section IDs and headline copy are present. Remaining: I have not done a full visual browser QA pass, so the only follow-up would be checking final spacing/animation feel in a browser if you want pixel-level polish.
