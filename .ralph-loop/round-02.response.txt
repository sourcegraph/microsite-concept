Updated [index.html](/Users/asridhar/work/Sourcegraph/projects/microsite/index.html) with a tighter second pass rather than a rebuild. The MCP and Sourcegraph sections now use Sourcegraph’s current MCP model more concretely: real tool names like `list_repos`, `nls_search`, `read_file`, `go_to_definition`, `find_references`, `commit_search`, `diff_search`, and `deepsearch`; the correct `/.api/mcp` and `/.api/mcp/deepsearch` endpoint split; OAuth/access-token auth guidance; and a clearer supported-hosts summary. I also added a dedicated “Sourcegraph MCP Guide” block inside the MCP chapter so the page explains how an agent should gather context step by step, not just what MCP is.

Text-level verification is complete and the new guide/content blocks are present in the document. Remaining: I was not able to complete browser automation QA in this round because the local Playwright browser action was cancelled, so spacing/animation/rendering still needs a live browser pass if you want final visual polish.